package com.mycompany.poojava;

interface Interface { //todos métodos são abstratos

    void interfaces();
    
    void imprimir(); //Dependency Inversion Principle: depender de Interface, ñ de classes
}
